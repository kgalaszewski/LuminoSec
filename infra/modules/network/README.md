# network (placeholder)

Not built yet. Will hold the VPC, subnets, route tables, and
security groups needed once ECS Fargate / RDS are introduced.
The static-site module doesn't need a VPC (S3 + CloudFront are
not deployed inside one).
